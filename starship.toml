
format = """
[](#73daca)\
[](bg:#73daca fg:#73daca)\
$directory\
[](fg:#73daca bg:#f5a97f)\
$git_branch\
$git_status\
[](fg:#f5a97f bg:#7aa2f7)\
$c\
$elixir\
$elm\
$golang\
$haskell\
$java\
$julia\
$nodejs\
$nim\
$rust\
[](fg:#7aa2f7 bg:#9ece6a)\
$docker_context\
[](fg:#9ece6a bg:#f7768e)\
$time\
[ ](fg:#f7768e)\
"""
# c30010
# Disable the blank line at the start of the prompt
# add_newline = false

# You can also replace your username with a neat symbol like  to save some space
[username]
show_always = true
style_user = "bg:#bb9af7 fg:#000000"
style_root = "bg:#bb9af7 fg:#000000"
format = '[$user ]($style)'

[directory]
style = "bg:#73daca fg:#000000"
format = "[ $path ]($style)"
truncation_length = 3
truncation_symbol = "…/"

# Here is how you can shorten some long paths by text replacement
# similar to mapped_locations in Oh My Posh:
[directory.substitutions]
"Documents" = " "
"Downloads" = " "
"Music" = " "
"Pictures" = " "
# Keep in mind that the order matters. For example:
# "Important Documents" = "  "
# will not be replaced, because "Documents" was already substituted before.
# So either put "Important Documents" before "Documents" or use the substituted version:
# "Important  " = "  "

[c]
symbol = " "
style = "bg:#7aa2f7 fg:#000000"
format = '[ $symbol ($version) ]($style)'

[docker_context]
symbol = " "
style = "bg:#9ece6a fg:#000000"
format = '[ $symbol $context ]($style) $path'

[elixir]
symbol = " "
style = "bg:#7aa2f7 fg:#000000"
format = '[ $symbol ($version) ]($style)'

[elm]
symbol = " "
style = "bg:#7aa2f7 fg:#000000"
format = '[ $symbol ($version) ]($style)'

[git_branch]
symbol = ""
style = "bg:#ff9e64 fg:#000000"
format = '[ $symbol $branch ]($style)'

[git_status]
style = "bg:#ff9e64 fg:#000000"
format = '[$all_status$ahead_behind ]($style)'

[golang]
symbol = " "
style = "bg:#7aa2f7 fg:#000000"
format = '[ $symbol ($version) ]($style)'

[haskell]
symbol = " "
style = "bg:#7aa2f7 fg:#000000"
format = '[ $symbol ($version) ]($style)'

[java]
symbol = " "
style = "bg:#7aa2f7 fg:#000000"
format = '[ $symbol ($version) ]($style)'

[julia]
symbol = " "
style = "bg:#7aa2f7 fg:#000000"
format = '[ $symbol ($version) ]($style)'

[nodejs]
symbol = ""
style = "bg:#7aa2f7 fg:#000000"
format = '[ $symbol ($version) ]($style)'

[nim]
symbol = " "
style = "bg:#7aa2f7 fg:#000000"
format = '[ $symbol ($version) ]($style)'

[rust]
symbol = ""
style = "bg:#7aa2f7 fg:#000000"
format = '[ $symbol ($version) ]($style)'

[time]
disabled = true 
time_format = "%R" # Hour:Minute Format
style = "bg:#f7768e fg:#000000"
format = '[ $time ]($style)'

